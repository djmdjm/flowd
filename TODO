- support relaying of pre/post-filter packets to another agent (via UDP)
- runtime reconfigure
  - parse config in monitor, deep-copy to child
- don't pass sockaddr_storage pointers around, just use (struct sockaddr *)
